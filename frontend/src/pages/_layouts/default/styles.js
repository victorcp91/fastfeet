import styles from 'styled-components';

export const Wrapper = styles.div`
`;

export const Content = styles.main`
  width: calc(100% - 40px);
  max-width: 1200px;
  margin: 0 auto;
  padding: 32px 0;
`;
